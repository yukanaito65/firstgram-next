version: "3.9"
services:
  nextjs:
    container_name: "hotelsSite"
    build:
      dockerfile: "./Dockerfile"
    volumes:
      - "./:/usr/src/app"
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true
